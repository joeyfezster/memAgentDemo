{
  "name": "memagentdemo-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm test:reset-db && playwright test",
    "test:reset-db": "cd ../infra && docker compose run --rm db-init",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
