# Backend Service

FastAPI-based backend for the memAgent demo platform.

## Local development

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

## Testing

```bash
poetry run pytest
```
